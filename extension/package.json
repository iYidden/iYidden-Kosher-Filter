{
  "name": "kosher-filter-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension for the kosher filter (Phase 1 MVP).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "preact": "^10.22.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@preact/preset-vite": "^2.9.0",
    "@types/chrome": "^0.0.270",
    "@types/node": "^20.12.0",
    "typescript": "^5.5.0",
    "vite": "^8.0.12",
    "vitest": "^4.1.6"
  }
}
